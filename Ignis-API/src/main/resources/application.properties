spring.application.name=API

# MariaDB
spring.datasource.url=${DB_URL:jdbc:mariadb://localhost:3306/ignis}
spring.datasource.username=${DB_USERNAME:ignis}
spring.datasource.password=${DB_PASSWORD:qwe123}
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# JWT
jwt.secret=${JWT_SECRET:moj_super_tajny_bardzo_dlugi_klucz_do_podpisywania_tokenow_2026_ignis_backend}
jwt.expiration=${JWT_EXPIRATION:3600000}

# CORS
app.cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://192.168.*.*:5173}

server.port=${SERVER_PORT:8080}